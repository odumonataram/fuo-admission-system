# Requirements for Phases 1-2 (current). This is intentionally minimal so
# `pip install -r requirements.txt` is fast and has no system-library
# dependencies on Windows. See requirements-later-phases.txt for what
# gets added when we build QR codes, PDF letters, and Excel/CSV reports.

Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Mail==0.9.1
Flask-Limiter==3.8.0
WTForms==3.1.2
python-dotenv==1.0.1
email-validator==2.2.0

# Phase 5: QR code generation for admission letters
qrcode==7.4.2
Pillow==10.4.0
