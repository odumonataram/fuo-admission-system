# Added when we reach the corresponding phase — no need to install yet.

# Phase 5: QR code + PDF admission letter generation
qrcode==7.4.2
Pillow==10.4.0
WeasyPrint==62.3

# Phase 7: Report exports
openpyxl==3.1.5
pandas==2.2.3

# Production deployment (MySQL + WSGI server)
PyMySQL==1.1.1
cryptography==43.0.1
gunicorn==23.0.0
